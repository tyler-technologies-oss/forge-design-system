name: Build

on:
  pull_request:

jobs:
  build:
    name: Build
    uses: tyler-technologies-oss/forge-automation-shared/.github/workflows/wf-build-and-test.yml@v2.2.1
    secrets:
      NPM_TOKEN: ${{ secrets.FORGE_NPM_TOKEN }}
